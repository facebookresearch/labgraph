import React from 'react';
import { Graph } from '../../components';

const Home: React.FC = (): JSX.Element => {
    return (
        <React.Fragment>
            <Graph />
        </React.Fragment>
    );
};

export default Home;
