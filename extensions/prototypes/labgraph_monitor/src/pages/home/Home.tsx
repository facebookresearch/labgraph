import React from 'react';
import { Graph } from '../../components';

const Home: React.FC = () => {
    return (
        <React.Fragment>
            <Graph />
        </React.Fragment>
    );
};

export default Home;
